# מניינים קרובים

אפליקציית אנדרואיד (תומכת גם במכשירי מקשים ישנים, Android 4.4+) שמציגה
אילו מניינים מתחילים עכשיו באזור המשתמש, איזה מניין התחיל לאחרונה, ורשימה
מלאה ומפורטת של כל המניינים היום - לפי קובץ נתונים (JSON) שהמשתמש טוען.

## הרצה / בנייה

הפרויקט נבנה עם Gradle סטנדרטי. כדי לבנות:

1. פתחו את התיקייה ב-Android Studio - הוא ייצור אוטומטית את ה-Gradle
   Wrapper החסר (`gradlew`, `gradle-wrapper.jar`) בסנכרון הראשון.
   לחלופין, מריצים פעם אחת `gradle wrapper --gradle-version 8.7` מתוך
   תיקיית הפרויקט אם יש Gradle מותקן מקומית.
2. `./gradlew assembleDebug` - יוצר APK תחת
   `app/build/outputs/apk/debug/`.
3. שני קובצי ה-CI (`.github/workflows/build.yml` ו-`.gitlab-ci.yml`)
   בונים APK אוטומטית בכל push, ומעלים אותו כ-artifact להורדה.

## פורמט קובץ הנתונים (JSON)

האפליקציה טוענת קובץ יחיד (כפתור "טעינת קובץ מניינים" במסך הראשי) בפורמט:

```json
{
  "shuls": [
    {
      "name": "חניכי הישיבות - חברון",
      "nusach": "אשכנז",
      "prayers": {
        "weekday": { "shacharit": "06:30", "mincha": "13:15", "arvit": "20:00" },
        "shabbat": { "shacharit": "08:30", "mincha": "13:30" }
      }
    },
    {
      "name": "אור לישרים",
      "nusach": "ע\"מ",
      "prayers": {
        "weekday": { "shacharit": "07:00", "arvit": "19:50" }
      }
    }
  ]
}
```

- כל שדה תפילה (`shacharit`/`mincha`/`arvit`) הוא **אופציונלי** - אפשר
  להצהיר רק על התפילות שרלוונטיות לבית כנסת מסוים.
- השעות הן קבועות (`HH:mm`, 24 שעות) - לא זמנים הלכתיים (זריחה/שקיעה).
- סיווג "שבת" מול "יום חול" נקבע לפי יום השבוע במכשיר (יום שבת = שבת,
  כל שאר הימים = חול), כפי שהתבקש.
- קובץ ה-HTML הנלווה (`יצירת-קובץ-מניינים.html`) בונה קובץ בפורמט הזה
  בלי צורך לכתוב JSON ידנית.

## התראות

במסך "הגדרות" אפשר להפעיל עד 3 התראות יומיות בשעה קבועה - אחת לכל סוג
תפילה (שחרית/מנחה/ערבית). כל התראה מציגה את המניין הקרוב ביותר מאותו סוג
תפילה, כולל שם בית הכנסת והנוסח. ההתראות שורדות אתחול מכשיר (`BootReceiver`)
ומתוזמנות מחדש אוטומטית ל-24 שעות קדימה בכל הפעלה, כך שדיוק השעה נשמר
לאורך זמן.

מומלץ ללחוץ גם על הכפתור "ביטול חיסכון בסוללה" במסך ההגדרות - חלק
מהיצרנים (Xiaomi/MIUI, Samsung ועוד) עלולים להשהות התראות מתוזמנות בלי
זה, גם עם כל שאר ההגנות תקינות.
